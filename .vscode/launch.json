{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Zenith (release)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/target/release/zenith",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "zenith: build release",
      "environment": [
        {
          "name": "DISPLAY",
          "value": ":1"
        }
      ],
      "externalConsole": false
    },
    {
      "name": "Zenith (debug)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/zenith",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "zenith: build debug",
      "environment": [
        {
          "name": "DISPLAY",
          "value": ":1"
        }
      ],
      "externalConsole": false
    }
  ]
}